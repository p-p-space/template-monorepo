# @space/tailwind-config

Config/theme compartido para Tailwind CSS v4 en el monorepo.

## Uso en una app

```css
@import "@space/tailwind-config/styles.css";
```

Luego puedes extender con estilos propios de la app.
